#!/bin/bash

sudo apt-get install zsh \
                     nmap \
                     git-flow \
                     python-dev \
                     build-essential \
                     ripgrep

[[ ! $SHELL =~ "zsh" ]] && chsh -s `which zsh`
